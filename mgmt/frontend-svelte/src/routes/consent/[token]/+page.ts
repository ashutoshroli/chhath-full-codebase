// Public consent page — dynamic token route resolved client-side via the SPA
// 200.html fallback (no prerender, no SSR). Matches the React /consent/:token.
export const prerender = false;
export const ssr = false;
