// Public announcement portal — dynamic token route resolved client-side via the
// SPA 200.html fallback (no prerender, no SSR). Matches React /announce/:token.
export const prerender = false;
export const ssr = false;
